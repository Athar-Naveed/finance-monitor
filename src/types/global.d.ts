// Types
interface Feature {
  icon: React.ElementType;
  title: string;
  description: string;
}

interface Step {
  icon: React.ElementType;
  title: string;
  description: string;
}

interface Stat {
  icon: React.ElementType;
  value: string;
  label: string;
}
